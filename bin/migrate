#!/bin/bash
source ./bin/generateParams
function execute() 
{
	command="java $* -cp build/libs/cassandra-migration-all.jar physicalgraph.migration.Migrate"
	echo $command
	$command
}

params="$(generateParams $*)"
execute $params
